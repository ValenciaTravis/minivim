#include <bits/stdc++.h>
#include <multiTrie.h>
#include <utils.h>

using namespace std;

int main(){

    // auto ret = utils::findNxtWord("e", 0);
    // printf("%d %d\n", ret.first, ret.second);

    // multiTrie tr;

    // vector<string> keywords = {"if", "else", "while", "signed", "throw", "union", "this", "int", "char", "double",
    //             "unsigned", "const", "goto", "virtual", "for", "float", "break", "auto", "class", 
    //             "operator", "case", "do", "long", "typedef", "static", "friend", "template", "default",
    //             "new", "void", "register", "extern", "return", "enum", "inline", "try", "short", "continue",
    //             "sizeof", "switch", "private", "protected", "asm", "catch", "delete", "public", "volatile", 
    //             "struct", "include", "using", "namespace"};
    // // for(auto& word : keywords) printf("%s\n", word.c_str());
    // // puts("---");
    // for(auto& word : keywords) tr.insert(word);

    // // for(int i=1;i<=tr.size;i++) 
    // //     printf("str = %s, siz = %d\n", tr.a[i].s.c_str(), tr.a[i].siz);

    // // printf("size = %d\n", tr.size);

    // vector<string> v = tr.findPrefix("i");

    // for(auto str : v) printf("%s\n", str.c_str());

    // // tr.del("123");

    // // v = tr.findPrefix("", 1);

    // // for(auto str : v) printf("%s\n", str.c_str());
    
}